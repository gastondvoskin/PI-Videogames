import { Link } from "react-router-dom";

const Card = () => {
    return (
        <div>
            NIY: Card
            <button>
                <Link to="/detail/1">/detail/1 (dev)</Link>
            </button>
        </div>
    );
};

export default Card; 