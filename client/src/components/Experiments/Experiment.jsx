import styles from "./Experiment.module.css";

const Experiment = () => {
    return (
        <div className={styles.mainContainer}>This is just an experiment</div>
    );
};

export default Experiment;